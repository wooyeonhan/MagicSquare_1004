# Magic Square (4×4) — 제품 요구사항 정의서 (PRD) 보고서

**작성 목적**: 구현·테스트·리뷰의 단일 기준선으로 쓰는 PRD를 Report 시리즈에 포함·배포한다.  
**범위**: 요구·계약·성공 기준·NFR·문서 추적 맵. 구현 코드는 포함하지 않는다.

---

## 메타 정보

| 항목 | 내용 |
|------|------|
| 주제 | MagicSquare 제품 요구사항 정의서(PRD) |
| 선행 문서 | `01.MagicSquare_ProblemDefinition_Report.md`, `02.MagicSquare_CleanArchitecture_TDD_Design_Report.md`, `03.MagicSquare_CursorRules_Report.md`, `04.MagicSquare_UserJourney_Epic_To_Technical_Report.md` |
| 동기 원본 | 워크스페이스 `docs/PRD.md`와 동일 계열; 변경 시 한쪽을 정본으로 두고 상호 동기화한다. |

---

# Magic Square (4×4) — 제품 요구사항 정의서 (PRD)

| 항목 | 내용 |
|------|------|
| 제품명 | MagicSquare (4×4 마방진 퍼즐, 빈칸 2개) |
| 문서 목적 | 구현·테스트·리뷰의 단일 기준선(요구·계약·성공 기준) |
| 정본·근거 | `Report/` 및 `Prompting/` 산출과 정렬; 상세 설계는 Report를 따른다 |

---

## 1. 배경과 비전

### 1.1 문제 인식 (요약)

4×4 격자에 1~16을 한 번씩 배치할 때 행·열·두 주 대각선의 합이 모두 같아야 하는 **마방진**을 다룬다. 본 프로젝트는 “앱 화면”보다 **규칙·불변조건·입출력 계약을 먼저 고정하고, TDD로 검증 가능한 명세로 내리는 것**을 목표로 한다. (상세: `Report/01.MagicSquare_ProblemDefinition_Report.md`)

### 1.2 제품 비전

**Invariant(불변조건) 기반 사고**와 **Dual-Track TDD**(도메인 트랙 + 경계 계약 트랙)를 연습할 수 있는 **학습·측정 가능한** 소프트웨어 자산을 만든다. (Epic: `Report/04.MagicSquare_UserJourney_Epic_To_Technical_Report.md`)

### 1.3 왜 프로그램·TDD인가 (요약)

- 반복 가능한 실행, 판정 자동화, 실수 조기 발견, 규칙을 실행 가능한 형태로 고정. (`Report/01` STEP 3~4)
- 입력·출력·실패가 명확해야 테스트가 의미를 갖고, 회귀와 계약이 통제된다.

---

## 2. 목표와 성공 기준

### 2.1 비즈니스·학습 목표

1. Invariant 중심 설계(명명된 INV-*와 검증 시점 고정).
2. Dual-Track TDD(ECB, Boundary ↔ Domain 계약 분리).
3. 입력·출력 계약의 명확화 및 테스트와의 1:1 추적.
4. 설계 → 테스트 → 구현 → 리팩터 단계 분리 체화.

### 2.2 측정 가능한 성공 기준 (PRD 수준)

| 구분 | 기준 |
|------|------|
| 도메인 신뢰도 | Domain(entity 등 순수 도메인) 라인 커버리지 ≥ 95% |
| 입력 경계 | 입력 검증 관련 계약·단위 테스트 100% 통과 |
| 코드 품질 | 의미 없는 하드코딩·매직 넘러 남용 없음(명명 상수·설정 집약) |
| 추적성 | INV / 계약 ID ↔ 테스트 ID 매핑 가능 |

(상세 매트릭스: `Report/02.MagicSquare_CleanArchitecture_TDD_Design_Report.md` §4.5)

### 2.3 범위 메모

- **포함:** ECB, 계약 테스트 우선, 리팩터 단계, 커버리지·추적성.
- **비우선(별도 범위):** UI 화면 품질, 온라인 배포, 알고리즘 최적화 경쟁.

---

## 3. 이해관계자·페르소나

| 역할 | 설명 |
|------|------|
| 주 사용자 | 소프트웨어 개발 학습자(TDD·Clean Architecture/ECB 학습 중) |
| 목표 | “앱 완성”이 아니라 계약·불변·트랙 분리 습관화 |

---

## 4. 핵심 제품 요구사항 (기능)

### 4.1 입력 (고정 계약)

- **형식:** `4×4` 정수 격자; `0`은 빈칸.
- **도메인 제약:** 빈칸(`0`)은 **정확히 2개**; 각 칸은 `0` 또는 `1..16`; `0`을 제외한 값은 **중복 없음**.

### 4.2 출력 (고정 계약)

- **형식:** 길이 6의 정수 배열 `int[6]`: `[r1, c1, n1, r2, c2, n2]`.
- **좌표:** **1-index** (1~4).
- **`n1`, `n2`:** 두 누락 숫자. **(작은 수 → 첫 빈칸, 큰 수 → 둘째 빈칸)** 조합이 마방진이면 그 순서; 아니면 **반대 순서**가 계약에 맞는 출력이다.

### 4.3 빈칸 순서 규칙 (고정)

두 빈칸 `(r_a, c_a)`, `(r_b, c_b)`에 대해 **행 우선**: `r_a < r_b` 이거나 `(r_a = r_b` 이고 `c_a < c_b)`이면 첫 빈칸은 `(r_a, c_a)`, 둘째는 `(r_b, c_b)`. 내부는 0-index 가능하나 **출력 직전에만** 1-index 변환.

### 4.4 핵심 동작

1. **입력 검증:** 스키마·불변 위반 시 도메인 솔브로 진행하지 않는다.
2. **빈칸 탐색:** 위 순서로 정확히 2좌표.
3. **누락 수:** `{1..16}`에서 비어 있는 두 수 `(min, max)`.
4. **마방진 판정:** 완전 채움에 대해 행·열·두 주 대각선 합 동일 및 (계약상) 1~16 퍼뮤테이션 검증.
5. **솔브:** 두 배치 시도 후 계약 배열 반환; 둘 다 불가 시 정의된 **불가** 결과(경계 에러 스키마).

### 4.5 경계(외부) 실패 계약 (요약)

호출자 관점에서 크기·빈칸 개수·값 범위·비0 중복·도메인 불가 등에 대해 **안정적인 `code` + 고정 `message`**를 제공한다. 문구·코드 표는 `Report/02` §2.2~2.4 및 `Prompting/`의 UI 계약과 동일하게 유지한다(Breaking 변경 시 프로세스·테스트 정책 준수).

---

## 5. 도메인 불변조건 (요약)

| ID | 요약 |
|----|------|
| INV-GRID-SIZE | 항상 4×4 |
| INV-EXACTLY-TWO-ZEROS | `0`인 칸 정확히 2개 |
| INV-DOMAIN-VALUES | `{0} ∪ {1..16}` |
| INV-NO-DUP-NONZERO | 비0 중복 없음 |
| INV-MAGIC-CONSTANT | 완전 채움 시 행·열·주대각 합 동일(1~16 사용 시 34) |
| INV-PERMUTATION-WHEN-FILLED | 완전 채움 시 1~16 각 1회 |
| INV-SOLUTION-ORDER | 출력 `(n1,n2)`와 빈칸·작은/큰 배치 규칙 일치 |

---

## 6. 아키텍처·구현 원칙 (요구사항)

- **패턴:** ECB(Entity–Control–Boundary). 의존: `boundary → control → entity` 단방향.
- **언어·품질:** Python 3.10+, PEP 8, 타입 힌트·Google 스타일 docstring(public), 줄 길이 88.
- **TDD:** RED → GREEN → REFACTOR; 금지 사항·테스트 정책은 루트 `.cursorrules` 및 `Report/03.MagicSquare_CursorRules_Report.md`와 일치.

---

## 7. 데이터·통합 (선택·단계적)

- **목적:** 마지막 유효 입력·결과 재현(학습).
- **1차:** InMemory 저장소; **통합/연습:** 파일(JSON 등) 어댑터로 교체 가능한 포트.
- Domain은 저장소에 **비의존**; Application/Control이 저장 호출. (`Report/02` §3~4)

---

## 8. 사용자 스토리 요약 (수용 기준 PRD 링크)

| 스토리 | 한 줄 가치 |
|--------|------------|
| 입력 검증 | 불변 위반 입력이 Entity로 넘어가지 않음 |
| 빈칸 탐색 | 행 우선 순서로 2좌표 확정 |
| 누락 수 | (min, max) 두 수 확정 |
| 마방진 판정 | 완전 격자에 대한 참/거짓 |
| 두 조합 시도 | 작은→첫 / 실패 시 역순 → `int[6]` 또는 불가 |

상세 AC·Gherkin·fixture 기대값: `Report/04` Level 3~4.

---

## 9. 참조 Gherkin·Fixture (PRD 요약)

- **첫 배치 성공 예:** 보고서의 검증된 4×4 fixture; 기대 `int[6]` 예: `[1, 2, 3, 1, 4, 13]` (1-index, 계약 순서).
- **역배치만 성공 예:** 기대 `int[6]`: `[3, 3, 6, 4, 4, 1]`.

(전문: `Report/04` Level 4)

---

## 10. 비기능 요구사항 (NFR)

| 영역 | 요구 |
|------|------|
| 결정성 | 동일 입력 → 동일 출력(비결정적 RNG에 의존하지 않음) |
| 테스트 | pytest, AAA, 최소 커버리지 80%(프로젝트 정책) |
| 관측성 | 디버깅용 `print()` 금지; `logging` 등 경계에 맞는 방식 |
| 유지보수 | 매직 넘버 산재 금지; 구체적 예외 처리 정책 |

---

## 11. 추적성·문서 맵

| PRD 섹션 | 주요 근거 문서 |
|----------|----------------|
| 문제·불변·TDD 동기 | `Report/01.MagicSquare_ProblemDefinition_Report.md` |
| 계약·레이어·테스트·Data·통합 | `Report/02.MagicSquare_CleanArchitecture_TDD_Design_Report.md` |
| AI·코딩 규약 | `Report/03.MagicSquare_CursorRules_Report.md`, `.cursorrules` |
| Epic·Journey·Story·Gherkin·검증 | `Report/04.MagicSquare_UserJourney_Epic_To_Technical_Report.md` |
| 대화·프롬프트 유래 | `Prompting/01`~`04` (동일 주제의 프롬프트·응답 스냅샷) |

---

## 12. 용어

- **마방진(본 PRD):** 4×4, 1~16 각 1회, 네 행·네 열·두 주 대각선 합이 모두 동일.
- **빈칸:** 값 `0`.
- **계약:** 본 문서 §4 및 `Report/02`의 스키마·에러 표와 동일 해석.

---

## 문서 이력

| 버전 | 날짜 | 요약 |
|------|------|------|
| 1.0 | 2026-04-27 | Report 01~04 및 Prompting 01~04 통합 PRD 초안 |
| 1.0 (Report) | 2026-04-27 | `Report/05.MagicSquare_PRD_Report.md`로보냄; `docs/PRD.md`와 본문 동기 |
